/*#include <Windows.h>
#include <iostream>
#include <string>
#include <gl/GL.h>
#include <gl/GLU.h>
#include "GL\freeglut.h"

using namespace std;

static int ProduceShader(const string& vertexShader, const string& fragmentShader, int argc, char* argv[])
{
	glutInit(&argc, argv);
	glutInitContextVersion(3, 3);
	glutInitContextProfile(GLUT_CORE_PROFILE);
	unsigned int program; //= glCreateProgram();
	unsigned int vs; //= glCreateShader(GL_VERTEX_SHADER);

	#define GL_POSITION = 0, 0, 0;

}

int mainImage()
{

}*/


//All of this was my attempt at implmenting a shader before we got to the Week 8 lecture.